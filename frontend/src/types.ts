export type EngineState = 'stopped'|'starting'|'loading'|'ready'|'stopping'|'failed'|'external'
export interface EngineStatus { state:EngineState; mode:string; owned:boolean; model?:string; modelPath?:string; pid?:number; startedAt?:number; exitCode?:number; error?:string; phase?:string; progress?:{doneBytes:number;totalBytes:number;percent?:number}; health?:Record<string,any>; capabilities?:{chat:boolean;monitoring:boolean;localModels:boolean;downloads:boolean;lifecycle:boolean;deleteModels:boolean} }
export interface Model { id:string;name:string;path:string;repository?:string;architecture:string;quantization?:string;parameterCount?:string;activeParameterCount?:string;sizeBytes:number;status:string;compatibility:'verified'|'likely'|'unknown';issue?:string;modifiedAt:number }
export interface Metrics {timestamp:number;error?:string;engineFootprint?:{ramBytes?:number;vramBytes?:number};system:{cpu:{percent:number;cores:number;load:number[]};ram:{usedBytes:number;availableBytes:number;totalBytes:number;percent:number};storage:{usedBytes:number;freeBytes:number;totalBytes:number;percent:number};gpus:Array<{index:number;name:string;uuid:string;utilization:number;memoryUsedBytes:number;memoryTotalBytes:number;memoryPercent:number;temperatureC:number;powerWatts:number}>};runtime:any;history:Array<{timestamp:number;cpu:number;ram:number;gpu?:number;vram?:number;decodeTps:number;latency:number}>}
export interface ChatSummary {id:string;title:string;model?:string;systemPrompt:string;settings:Record<string,any>;createdAt:number;updatedAt:number}
export interface Message {id?:string;role:'system'|'user'|'assistant';content:string;reasoning?:string;usage?:any;createdAt?:number}
